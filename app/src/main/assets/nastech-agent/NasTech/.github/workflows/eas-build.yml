# NOTE: This file lives at NasTech/.github/workflows/ which GitHub Actions does NOT scan.
# GitHub only reads workflows from the repo root .github/workflows/ directory.
#
# The canonical active EAS Build workflow is at:
#   .github/workflows/eas-build.yml  (repo root)
#
# Do NOT edit this file. Edit .github/workflows/eas-build.yml instead.
name: EAS Build (reference — not active)

on:
  workflow_dispatch:
    inputs:
      _note:
        description: 'NOT ACTIVE — see .github/workflows/eas-build.yml at repo root'
        required: false

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "This file is in NasTech/.github/workflows/ and is NOT picked up by GitHub Actions."
          echo "The active EAS Build workflow is at: .github/workflows/eas-build.yml (repo root)"
